use crate::io::{Input, Metric, Props};

/**
 * main solver function
 */
pub fn solve(input: &Input, props: &Props) -> Result<Metric, String> {
    todo!();
    // Ok(Metric{
    //     elapsed: 0.0,
    //     score: 0,
    // })
}